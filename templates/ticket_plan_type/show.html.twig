{% extends 'base.html.twig' %}

{% block title %}TicketPlanType{% endblock %}

{% block body %}
    <h1>TicketPlanType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticket_plan_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ticket_plan_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ticket_plan_type_index') }}">back to list</a>

    <a href="{{ path('ticket_plan_type_edit', {'id': ticket_plan_type.id}) }}">edit</a>

    {{ include('ticket_plan_type/_delete_form.html.twig') }}
{% endblock %}