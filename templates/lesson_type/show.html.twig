{% extends 'base.html.twig' %}

{% block title %}LessonType{% endblock %}

{% block body %}
    <h1>LessonType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lesson_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lesson_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lesson_type_index') }}">back to list</a>

    <a href="{{ path('lesson_type_edit', {'id': lesson_type.id}) }}">edit</a>

    {{ include('lesson_type/_delete_form.html.twig') }}
{% endblock %}